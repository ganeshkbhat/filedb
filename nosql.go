package nosql

// package nosql

import (
	// "errors"
	// "fmt"
	// "os"
	// "reflect"
)

// // add two numbers
func add(a, b int) int {
	return a + b
}

//
// func main() {
// 	if len(os.Args) < 2 {
// 		errors.New("apply: not a function")
// 	}
// 	// Get the command line arguments.
// 	fmt.Println("os.Args: ", os.Args)
// 	// Get the command line arguments.
// 	args := os.Args[1:]
// 	fmt.Println("os.Args[1:]: ", args)
// 	// Call the function specified in the first argument with the rest of the arguments.
// 	if len(args) > 0 {
// 		fmt.Printf("Argument %v type is %v\n", args[0], reflect.TypeOf(args[0]))
// 		fmt.Printf("Argument %v type is %v\n", args[1], reflect.TypeOf(args[1]))
// 		fmt.Printf("Argument %v type is %v\n", args, reflect.TypeOf(args))
// 	}
// }
//
